# requirements.txt - Railway Build Optimized (keeps Docling)

# Core FastAPI - Install first for better caching
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
python-dotenv==1.0.0

# Essential async and database
aiofiles==23.2.0
asyncpg==0.29.0
sqlalchemy[asyncio]==2.0.23

# AI processing core
openai==1.3.7
pydantic==2.5.0
pydantic-settings==2.1.0

# Authentication and security (lightweight)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Monitoring and utilities
structlog==23.2.0
tenacity==8.2.3
httpx==0.25.2

# Database tools
alembic==1.13.0
psycopg2-binary==2.9.9

# Document processing
python-magic==0.4.27
python-docx==0.8.11

# Docling - Keep but install efficiently
docling>=1.0.0
docling-core>=1.0.0
pymupdf==1.23.8

# Optional performance (install last)
redis==5.0.1
celery==5.3.4

# Build optimization
wheel==0.42.0